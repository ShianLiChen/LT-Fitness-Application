name: Selenium Tests (Chrome)

on:
  push:
    branches: [ "main", "TDev" ]
  pull_request:
    branches: [ "main", "TDev" ]

jobs:
  selenium-test-chrome:
    name: Run Selenium Tests on Chrome
    runs-on: ubuntu-latest

    services:
      selenium:
        image: selenium/standalone-chrome:latest
        options: --shm-size=2gb
        ports:
          - 4444:4444

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install selenium seleniumbase pytest

      - name: Create Dummy .env
        run: |
          echo "SECRET_KEY=testing_key" >> .env
          echo "JWT_SECRET_KEY=testing_key" >> .env
          echo "MYSQL_HOST=127.0.0.1" >> .env
          echo "MYSQL_PORT=3306" >> .env
          echo "MYSQL_USER=root" >> .env
          echo "MYSQL_PASSWORD=password" >> .env
          echo "MYSQL_DB=fitness_app_test" >> .env
          echo "FLASK_ENV=development" >> .env
          echo "FLASK_PORT=5000" >> .env
          # Browser in the selenium service will reach the runner via host.docker.internal
          echo "APP_URL=http://host.docker.internal:5000" >> .env

      - name: Start Flask App
        run: |
          export FLASK_APP=src/app.py
          flask run --host=0.0.0.0 --port=5000 &
          for i in {1..20}; do
            if curl -sS http://127.0.0.1:5000/ >/dev/null 2>&1; then
              echo "Flask is up"
              break
            fi
            echo "Waiting for Flask... ($i)"
            sleep 2
          done

      - name: Wait for Selenium service to be ready
        run: |
          echo "Waiting for Selenium at http://127.0.0.1:4444/wd/hub/status ..."
          for i in {1..30}; do
            if curl -sS http://127.0.0.1:4444/wd/hub/status | grep -q '"ready":true'; then
              echo "Selenium is ready"
              break
            fi
            echo "Waiting for Selenium... ($i)"
            sleep 1
          done

      - name: Debug- register page snippet (runner view)
        run: |
          echo "Register page snippet (runner view):"
          curl -sS http://127.0.0.1:5000/register | head -c 400 || true

      - name: Run Selenium Tests (remote Selenium service)
        env:
          PYTHONPATH: src
          SELENIUMBROWSER: chrome
          # App URL reachable from the selenium container
          APP_URL: http://host.docker.internal:5000
          # Recommended CI flags (SeleniumBase will pass these to Chrome)
          SBROWSER_ARGS: "--no-sandbox --disable-dev-shm-usage --disable-gpu --headless=new"
          # Use localhost for the Selenium server (runner -> service port mapping)
          SELENIUM_REMOTE_URL: http://127.0.0.1:4444/wd/hub
        run: |
          echo "Using remote Selenium server: $SELENIUM_REMOTE_URL"
          # Run tests against the remote Selenium server (point to localhost:4444)
          pytest tests/test_selenium_*.py --headless --browser=chrome --server=http://127.0.0.1:4444/wd/hub
